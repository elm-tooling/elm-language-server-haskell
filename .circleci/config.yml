version: 2
jobs:
  build:
    docker:
      - image: fpco/stack-build:lts-11.22
    steps:
      - checkout
      - run:
          name: Setup
          command: stack setup
      - run:
          name: Install
          command: stack install
